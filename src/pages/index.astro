---
import TallyInline from '../components/TallyInline.astro';
import Testimonials from '../components/Testimonials.astro';
import BookingCTA from '../components/BookingCTA.astro';
import AnalyticsPlausible from '../components/AnalyticsPlausible.astro';
import Container from '../components/Container.astro';
import Panel from '../components/Panel.astro';
import Badge from '../components/Badge.astro';
import MiniMetric from '../components/MiniMetric.astro';
import offer from '../data/offer.json';
import '../styles/global.css';

const title = "PT Authority Hub - Your personal fitness coach";
const description = "Get a custom training plan in 24 hours. Professional coaching for serious results.";
const canonicalURL = "https://pt-authority-hub-landing.pages.dev";

const quizFormId = import.meta.env.PUBLIC_TALLY_QUIZ_FORM_ID || "OD4eKp";
const statsFormId = import.meta.env.PUBLIC_TALLY_STATS_FORM_ID || "pbyXyJ";
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="canonical" href={canonicalURL} />
  
  <!-- Open Graph / Social Media -->
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:type" content="website" />
  <meta property="og:url" content={canonicalURL} />
  
  <AnalyticsPlausible />
</head>
<body class="selection:bg-accent selection:text-bg">
  
  <!-- Skip to content link for accessibility -->
  <a href="#content" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-blue-600 focus:text-white focus:rounded">
    Skip to main content
  </a>
  
  <!-- Hero Section -->
  <section id="hero" class="min-h-screen flex items-center pt-24 pb-16 overflow-hidden relative">
    <!-- Subtle Background Glow -->
    <div class="absolute top-1/2 right-0 -translate-y-1/2 w-[500px] h-[500px] bg-accent/5 blur-[120px] rounded-full pointer-events-none"></div>
    
    <Container className="relative z-10">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 items-center">
        <!-- Left: Copy -->
        <div class="text-left flex flex-col justify-center">
          <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold mb-6 leading-[1.1] tracking-tight text-text">
            Stop Guessing. <br class="hidden sm:block" />
            Start Progressing. <br class="hidden sm:block" />
            Get Your <span class="text-accent">Data-Driven</span> Training Plan.
          </h1>
          <p class="text-lg sm:text-2xl text-muted mb-8 max-w-xl font-medium leading-relaxed sm:text-[18px]">
            Get a Custom Training Plan in 24 Hours.
          </p>
          
          <div class="flex flex-col items-start gap-4 mb-10">
            <a href="/quiz" class="inline-flex items-center justify-center bg-accent text-bg px-10 py-5 rounded-lg text-xl font-bold hover:brightness-110 transition-all transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-accent focus:ring-offset-2 focus:ring-offset-bg min-h-[60px] plausible-event-name=Start+Free+Audit">
              Start Free Audit
            </a>
            <p class="text-sm text-muted ml-2">No credit card required</p>
          </div>

          <div class="flex items-center gap-4 py-4 px-6 bg-panel/30 border border-white/5 rounded-2xl w-fit">
            <div class="flex -space-x-2">
              {[1,2,3].map(() => (
                <div class="w-8 h-8 rounded-full border-2 border-bg bg-panel flex items-center justify-center overflow-hidden">
                  <div class="w-full h-full bg-muted/20"></div>
                </div>
              ))}
            </div>
            <div class="text-sm">
              <span class="text-accent font-bold">⭐⭐⭐⭐⭐</span>
              <span class="text-muted block text-xs">Rated 4.9/5 by 200+ clients</span>
            </div>
          </div>
        </div>

        <!-- Right: PT Profile + Dashboard -->
        <div class="relative space-y-6">
          <div class="mb-4">
            <h2 class="text-xl font-bold text-text mb-2 tracking-tight">See your progress in real numbers</h2>
            <p class="text-sm text-muted">A clear dashboard showing exactly how you're performing.</p>
          </div>
          
          <!-- PT Profile Card -->
          <Panel className="border-accent/10 bg-panel/80 backdrop-blur-sm p-6 flex items-center gap-6">
            <div class="w-20 h-20 rounded-2xl bg-muted/20 overflow-hidden flex-shrink-0 border border-white/5">
              <img src="/coach.jpg" alt="Coach" class="w-full h-full object-cover opacity-50" onerror="this.style.display='none'" />
            </div>
            <div>
              <p class="text-xs font-bold text-accent uppercase tracking-widest mb-1">Lead coach</p>
              <h3 class="text-xl font-bold text-text">Mark</h3>
              <p class="text-xs text-muted">Performance coach, strength and conditioning</p>
            </div>
          </Panel>

          <Panel className="border-accent/10 bg-panel/80 backdrop-blur-sm">
            <div class="grid grid-cols-1 gap-4">
              <div class="flex items-center justify-between p-4 bg-bg/50 rounded-lg border border-white/5">
                <MiniMetric label="Sleep" value="94%" note="CONSISTENT" />
                <span class="text-base text-white/80 italic text-right pl-4">Better sleep = faster recovery</span>
              </div>
              <div class="flex items-center justify-between p-4 bg-bg/50 rounded-lg border border-white/5">
                <MiniMetric label="Training balance" value="1.2" note="OPTIMAL" />
                <span class="text-base text-white/80 italic text-right pl-4">No over/under-training</span>
              </div>
              <div class="flex items-center justify-between p-4 bg-bg/50 rounded-lg border border-white/5">
                <MiniMetric label="Recovery" value="88" note="READY" />
                <span class="text-base text-white/80 italic text-right pl-4">Know when to push vs. rest</span>
              </div>
            </div>
          </Panel>

          <ul class="hidden lg:grid grid-cols-2 gap-3 opacity-70">
            <li class="flex items-center gap-2 text-xs text-muted uppercase tracking-widest">
              <span class="text-accent">/</span> 30-day data analysis
            </li>
            <li class="flex items-center gap-2 text-xs text-muted uppercase tracking-widest">
              <span class="text-accent">/</span> Personalized roadmap
            </li>
            <li class="flex items-center gap-2 text-xs text-muted uppercase tracking-widest">
              <span class="text-accent">/</span> Professional coaching
            </li>
            <li class="flex items-center gap-2 text-xs text-muted uppercase tracking-widest">
              <span class="text-accent">/</span> Remote optimization
            </li>
          </ul>
        </div>
      </div>
    </Container>
  </section>
  
  <!-- Main Content -->
  <main id="content">


  <!-- The Why Methodology Section -->
  <section id="the-why" class="py-24 sm:py-32 bg-bg">
    <Container>
      <div class="text-center mb-16">
        <h2 class="text-xs font-bold text-accent uppercase tracking-[0.3em] mb-4">The Methodology</h2>
        <p class="text-3xl sm:text-4xl font-bold text-text">Why this works</p>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <Panel className="p-8 text-center flex flex-col items-center">
          <div class="w-16 h-16 mb-6 bg-accent/10 border border-accent/20 rounded-full flex items-center justify-center text-accent">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
          </div>
          <h3 class="text-xl font-bold mb-3">Precision Programming</h3>
          <p class="text-muted leading-relaxed text-lg sm:text-[18px]">AI-driven training plans that adapt to your progress every single week.</p>
        </Panel>
        <Panel className="p-8 text-center flex flex-col items-center">
          <div class="w-16 h-16 mb-6 bg-accent/10 border border-accent/20 rounded-full flex items-center justify-center text-accent">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 6l3 1m0 0l-3 9a5.002 5.002 0 006.001 0M6 7l3 9M6 7l6-2m6 2l3-1m-3 1l-3 9a5.002 5.002 0 006.001 0M18 7l3 9m-3-9l-6-2m0-2v2m0 16V5m0 16H9m3 0h3"></path></svg>
          </div>
          <h3 class="text-xl font-bold mb-3">Metabolic Tracking</h3>
          <p class="text-muted leading-relaxed text-lg sm:text-[18px]">Simple nutrition guidance synced with your activity for maximum energy.</p>
        </Panel>
        <Panel className="p-8 text-center flex flex-col items-center">
          <div class="w-16 h-16 mb-6 bg-accent/10 border border-accent/20 rounded-full flex items-center justify-center text-accent">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
          </div>
          <h3 class="text-xl font-bold mb-3">Neural Recovery</h3>
          <p class="text-muted leading-relaxed text-lg sm:text-[18px]">Mindfulness and rest protocols that ensure you never hit a plateau.</p>
        </Panel>
      </div>
    </Container>
  </section>

  <!-- How It Works Section -->
  <section id="how-it-works" class="py-24 sm:py-32">
    <Container>
      <div class="text-center mb-16">
        <h2 class="text-xs font-bold text-accent uppercase tracking-[0.3em] mb-4">The process</h2>
        <p class="text-3xl sm:text-4xl font-bold text-text">How it works</p>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <Panel className="text-left flex flex-col items-center text-center p-8">
          <div class="w-16 h-16 mb-6 bg-accent/10 border border-accent/20 rounded-full flex items-center justify-center text-accent">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
          </div>
          <h3 class="text-xl font-bold mb-3">1. Tell us about you</h3>
          <p class="text-base text-muted mb-2 font-bold uppercase tracking-widest italic">Takes 5 min</p>
          <p class="text-muted leading-relaxed text-lg sm:text-[18px]">Share your goals and fitness level in our quick intake protocol.</p>
        </Panel>
        <Panel className="text-left flex flex-col items-center text-center p-8 border-accent/20 bg-accent/5">
          <div class="w-16 h-16 mb-6 bg-accent/10 border border-accent/20 rounded-full flex items-center justify-center text-accent">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01"></path></svg>
          </div>
          <h3 class="text-xl font-bold mb-3">2. We build your plan</h3>
          <p class="text-base text-muted mb-2 font-bold uppercase tracking-widest italic">Within 24 hours</p>
          <p class="text-muted leading-relaxed text-lg sm:text-[18px]">Our coaches analyze your data and create your personalized program.</p>
        </Panel>
        <Panel className="text-left flex flex-col items-center text-center p-8">
          <div class="w-16 h-16 mb-6 bg-accent/10 border border-accent/20 rounded-full flex items-center justify-center text-accent">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
          </div>
          <h3 class="text-xl font-bold mb-3">3. Start training</h3>
          <p class="text-base text-muted mb-2 font-bold uppercase tracking-widest italic">Ready by day 2</p>
          <p class="text-muted leading-relaxed text-lg sm:text-[18px]">Follow your tailored workouts and start seeing measurable progress immediately.</p>
        </Panel>
      </div>
    </Container>
  </section>

  <!-- Pain & Value Bridge Section -->
  <section id="pain-value-bridge" class="py-24 bg-bg2/50 relative">
    <Container>
      <Panel className="max-w-4xl mx-auto overflow-hidden">
        <div class="grid grid-cols-1 md:grid-cols-2 divide-y md:divide-y-0 md:divide-x divide-white/5">
          <div class="p-8 md:p-12">
            <h3 class="text-xs font-bold text-accent uppercase tracking-widest mb-8">Common performance problems we solve</h3>
            <ul class="space-y-4">
              <li class="flex items-start gap-4 text-text text-lg sm:text-[18px]">
                <span class="text-accent">—</span> Not seeing results despite working out regularly?
              </li>
              <li class="flex items-start gap-4 text-text text-lg sm:text-[18px]">
                <span class="text-accent">—</span> Confused about what to eat and when?
              </li>
              <li class="flex items-start gap-4 text-text text-lg sm:text-[18px]">
                <span class="text-accent">—</span> Tired all the time even though you're "healthy"?
              </li>
            </ul>
          </div>
          <div class="p-8 md:p-12 bg-white/[0.02]">
            <h3 class="text-xs font-bold text-accent uppercase tracking-widest mb-8">Professional coaching</h3>
            <ul class="space-y-4">
              <li class="flex items-start gap-3 text-text text-base font-medium">
                <span class="text-accent">✓</span> Proven workout plans
              </li>
              <li class="flex items-start gap-3 text-text text-base font-medium">
                <span class="text-accent">✓</span> Smart training that prevents burnout
              </li>
              <li class="flex items-start gap-3 text-text text-base font-medium">
                <span class="text-accent">✓</span> Clear, step-by-step 30-day system
              </li>
            </ul>
          </div>
        </div>
      </Panel>
    </Container>
  </section>

  <!-- Quiz Section -->
  <section id="quiz" class="py-24 bg-bg2/30">
    <Container>
      <div class="max-w-2xl mx-auto">
        <!-- Next steps hint -->
        <div class="mb-12 text-center">
          <Badge variant="accent" className="mb-4">Step 1 of 2</Badge>
          <h2 class="text-3xl font-bold text-text mb-4">Tell us about you</h2>
          <p class="text-muted">Takes about 2 minutes. Complete this quick protocol to secure your 24h turnaround.</p>
          <div class="mt-6">
            <a href="/upload" class="text-xs text-accent uppercase tracking-widest hover:underline">Optional: Upload your data →</a>
          </div>
        </div>

        <div class="bg-panel border border-white/5 rounded-xl overflow-hidden p-8 shadow-2xl text-center">
          <p class="text-lg text-text mb-8 italic">Ready to see your performance metrics?</p>
          <a href="/quiz" class="inline-flex items-center justify-center bg-accent text-bg px-8 py-4 rounded-lg text-lg font-bold hover:brightness-110 transition-all">
            Start the 2-Minute Audit
          </a>
        </div>
      </div>
    </Container>
  </section>

  <!-- Stats Section -->
  <section id="stats" class="py-24 border-y border-white/5 bg-bg2/10 plausible-event-name=View+Stats+Upload">
    <Container>
      <div class="max-w-2xl mx-auto">
        <div class="flex justify-center mb-6">
          <Badge variant="muted">Optional but recommended</Badge>
        </div>
        <p class="text-center text-lg text-muted mb-6">
          Sync your 30-day training data for a 
          <strong class="text-text">custom training plan in 24 hours</strong>.
        </p>
        <div class="text-center mb-12">
          <a href="/quiz" class="text-xs text-accent uppercase tracking-widest hover:underline">← Back to Step 1</a>
        </div>
        
        <div class="bg-panel border border-white/5 rounded-xl overflow-hidden p-8 mb-12 shadow-2xl text-center">
          <p class="text-lg text-text mb-8 italic">Want even more precision?</p>
          <a href="/upload" class="inline-flex items-center justify-center bg-white/5 text-text border border-white/10 px-8 py-4 rounded-lg text-lg font-bold hover:bg-white/10 transition-all">
            Upload Your Stats
          </a>
        </div>
        
        <!-- Consent and Disclaimer -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div class="bg-bg2/50 border border-white/5 rounded-lg p-5">
            <h4 class="text-[10px] font-bold text-muted uppercase tracking-widest mb-2">Consent and privacy</h4>
            <p class="text-[11px] text-muted leading-relaxed uppercase tracking-tight">
              By submitting, you consent to sharing data for analysis. We delete all data from our systems within 30 days of delivery.
            </p>
          </div>
          
          <div class="bg-bg2/50 border border-white/5 rounded-lg p-5">
            <h4 class="text-[10px] font-bold text-muted uppercase tracking-widest mb-2">Disclaimer</h4>
            <p class="text-[11px] text-muted leading-relaxed uppercase tracking-tight">
              Our analysis is professional coaching advice, not medical clinical guidance. Please consult your physician.
            </p>
          </div>
        </div>
      </div>
    </Container>
  </section>

  <!-- Reviews Section -->
  <Testimonials />
  
  <!-- Pricing Section -->
  <section id="pricing" class="py-24 bg-bg2/50">
    <Container>
      <div class="text-center mb-16">
        <h2 class="text-xs font-bold text-accent uppercase tracking-[0.3em] mb-4">Investment Protocol</h2>
        <p class="text-3xl font-bold text-text mb-4">Transparent Pricing for Serious Results</p>
        <p class="text-muted">{offer.disclaimer}</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {offer.tiers.map((tier) => (
          <Panel className={`flex flex-col relative ${tier.highlight ? 'border-accent/40 bg-accent/5 ring-1 ring-accent/20' : 'border-white/5'}`}>
            {tier.highlight && (
              <div class="absolute -top-3 left-1/2 -translate-x-1/2 bg-accent text-bg text-[10px] font-bold uppercase tracking-widest px-3 py-1 rounded-full">
                Recommended
              </div>
            )}
            <div class="mb-8">
              <h3 class="text-lg font-bold text-text mb-2 uppercase tracking-widest">{tier.name}</h3>
              <div class="flex items-baseline gap-1 mb-4">
                <span class="text-4xl font-bold text-text">{tier.price}</span>
                <span class="text-muted text-sm capitalize">/{tier.period}</span>
              </div>
              <p class="text-base text-muted leading-relaxed">{tier.description}</p>
            </div>
            
            <ul class="space-y-4 mb-10 flex-grow">
              {tier.features.map((feature) => (
                <li class="flex items-start gap-3 text-sm text-muted">
                  <span class="text-accent">✓</span>
                  <span>{feature}</span>
                </li>
              ))}
            </ul>
            
            <a href="/quiz" class={`w-full text-center py-4 rounded-lg font-bold transition-all ${tier.highlight ? 'bg-accent text-bg hover:brightness-110' : 'bg-white/5 text-text hover:bg-white/10'}`}>
              {tier.cta}
            </a>
          </Panel>
        ))}
      </div>
    </Container>
  </section>

  <!-- Live Spots Remaining Section -->
  <section id="spots-remaining" class="py-12 border-y border-white/5 bg-panel/30">
    <Container>
      <div class="flex flex-col items-center">
        <div class="inline-flex items-center gap-4 px-6 py-3 rounded-full border border-accent/20 bg-accent/5 backdrop-blur-sm">
          <span class="flex h-2 w-2 relative">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-accent opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-accent"></span>
          </span>
          <span class="text-sm font-bold text-text uppercase tracking-widest leading-none">
            System Capacity: <span id="spots-count" class="text-accent">...</span> Spots Remaining
          </span>
          <span id="spots-updated" class="hidden sm:block text-[11px] text-muted uppercase tracking-widest font-mono">
            Updated: <span id="spots-time">...</span>
          </span>
        </div>
      </div>
    </Container>
  </section>

  <!-- Transformation Gallery Section -->
  <section id="transformations" class="py-24 bg-bg2/30">
    <Container>
      <div class="text-center mb-16">
        <h2 class="text-xs font-bold text-accent uppercase tracking-[0.3em] mb-4">Transformations</h2>
        <p class="text-3xl font-bold text-text">Real Results from Real People</p>
      </div>
      <div class="text-center p-12 bg-panel/30 border border-white/5 rounded-2xl">
        <p class="text-muted italic">Success stories coming soon. Be one of our first success cases.</p>
      </div>
    </Container>
  </section>

  <!-- FAQ Section (Rewritten) -->
  <section id="faq" class="py-24">
    <Container>
      <div class="max-w-3xl mx-auto">
        <h2 class="text-xs font-bold text-accent uppercase tracking-[0.3em] mb-12 text-center">Frequently asked questions</h2>
        <div class="space-y-4">
          <Panel className="border-white/5">
            <h3 class="font-bold text-text mb-3 tracking-tight text-base italic">Do I need to live in London?</h3>
            <p class="text-muted text-base leading-relaxed sm:text-[18px]">No. While we support London-based sessions, our digital coaching and training plans are delivered globally via our secure platform.</p>
          </Panel>
          <Panel className="border-white/5">
            <h3 class="font-bold text-text mb-3 tracking-tight text-base italic">Do I need fancy equipment?</h3>
            <p class="text-muted text-base leading-relaxed sm:text-[18px]">No. We build your plan around the equipment you already have access to, whether that's a full gym or just a pair of dumbbells at home.</p>
          </Panel>
          <Panel className="border-white/5">
            <h3 class="font-bold text-text mb-3 tracking-tight text-base italic">What if I'm a complete beginner?</h3>
            <p class="text-muted text-base leading-relaxed sm:text-[18px]">That's perfect. Our system is designed to meet you where you are and scale as you become more confident and capable.</p>
          </Panel>
          <Panel className="border-white/5">
            <h3 class="font-bold text-text mb-3 tracking-tight text-base italic">How much time do workouts take?</h3>
            <p class="text-muted text-base leading-relaxed sm:text-[18px]">Most workouts are designed to be completed in 45-60 minutes, though we can adjust this based on your specific daily schedule.</p>
          </Panel>
          <Panel className="border-white/5">
            <h3 class="font-bold text-text mb-3 tracking-tight text-base italic">Can I cancel anytime?</h3>
            <p class="text-muted text-base leading-relaxed sm:text-[18px]">Yes. We don't believe in trapping people in long-term contracts. You can manage your subscription directly from your dashboard.</p>
          </Panel>
          <Panel className="border-white/5">
            <h3 class="font-bold text-text mb-3 tracking-tight text-base italic">What if I have an injury?</h3>
            <p class="text-muted text-base leading-relaxed sm:text-[18px]">We take safety seriously. Your initial intake allows you to specify any injuries so we can adapt your movements accordingly.</p>
          </Panel>
        </div>
      </div>
    </Container>
  </section>

  <!-- Trust & Credibility Block -->
  <section id="trust" class="py-24 bg-bg2/50 border-t border-white/5">
    <Container>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-12 items-center">
        <div>
          <h2 class="text-xs font-bold text-accent uppercase tracking-[0.3em] mb-4">Credentials</h2>
          <p class="text-3xl font-bold text-text mb-6">Expertise You Can Trust</p>
          <ul class="space-y-4">
            <li class="flex items-start gap-3 text-muted leading-relaxed sm:text-[18px]">
              <span class="text-accent">✓</span> Certified Performance Specialist (Level 4)
            </li>
            <li class="flex items-start gap-3 text-muted leading-relaxed sm:text-[18px]">
              <span class="text-accent">✓</span> Accredited Nutrition & Metabolic Advisor
            </li>
            <li class="flex items-start gap-3 text-muted leading-relaxed sm:text-[18px]">
              <span class="text-accent">✓</span> 5,000+ Hours of On-Floor Coaching Experience
            </li>
          </ul>
        </div>
        <Panel className="p-8 border-accent/20 bg-accent/5">
          <div class="flex items-center gap-4 mb-6">
            <div class="w-12 h-12 bg-accent/10 rounded-full flex items-center justify-center text-accent">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path></svg>
            </div>
            <h3 class="font-bold text-xl">Security & Satisfaction</h3>
          </div>
          <p class="text-muted text-sm mb-6 leading-relaxed">Your data is stored with bank-level encryption and is never shared. We also offer a 7-day satisfaction guarantee: not happy? Full refund, no questions asked.</p>
          <div class="flex items-center gap-2">
            <Badge variant="muted">Privacy First</Badge>
            <Badge variant="muted">Secure TLS</Badge>
          </div>
        </Panel>
      </div>
    </Container>
  </section>

  </main>

  <!-- Footer -->
  <footer id="footer" class="py-12 border-t border-white/5 bg-bg">
    <Container>
      <div class="flex flex-col md:flex-row items-center justify-between gap-6">
        <p class="text-[12px] text-muted uppercase tracking-widest font-mono">
          &copy; {new Date().getFullYear()} PT Authority Hub. Professional coaching.
        </p>
        <div class="flex gap-6">
          <a href="/privacy" class="text-[12px] text-muted hover:text-accent uppercase tracking-widest font-mono transition-colors">Privacy</a>
          <a href="/terms" class="text-[12px] text-muted hover:text-accent uppercase tracking-widest font-mono transition-colors">Terms</a>
          <a href="#" class="text-[12px] text-muted hover:text-accent uppercase tracking-widest font-mono transition-colors">Status</a>
        </div>
      </div>
    </Container>
  </footer>

  <script>
    async function loadSpotsRemaining() {
      try {
        const response = await fetch('/api/spots');
        const data = await response.json();
        const spotsElement = document.getElementById('spots-count');
        const timeElement = document.getElementById('spots-time');
        
        if (spotsElement && data.spotsRemaining !== undefined) {
          spotsElement.textContent = data.spotsRemaining;
        }
        
        if (timeElement && data.updatedAt) {
          const date = new Date(data.updatedAt);
          timeElement.textContent = date.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
        }
      } catch (error) {
        console.error('Error loading spots data:', error);
        const spotsElement = document.getElementById('spots-count');
        if (spotsElement) {
          spotsElement.textContent = '3'; // Safe default
        }
      }
    }
    
    // Load spots when DOM is ready
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', loadSpotsRemaining);
    } else {
      loadSpotsRemaining();
    }
  </script>
  <script is:inline>
    (function () {
      var d = document;
      var w = "https://tally.so/widgets/embed.js";

      function v() {
        if (typeof Tally !== "undefined") {
          Tally.loadEmbeds();
          return;
        }
        d.querySelectorAll("iframe[data-tally-src]:not([src])").forEach(function (e) {
          e.src = e.dataset.tallySrc;
        });
      }

      if (typeof Tally !== "undefined") {
        v();
        return;
      }

      if (d.querySelector('script[src="' + w + '"]') == null) {
        var s = d.createElement("script");
        s.src = w;
        s.onload = v;
        s.onerror = v;
        d.body.appendChild(s);
      }
    })();
  </script>

</body>
</html>
